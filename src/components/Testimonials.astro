---
const testimonials = [
  {
    quote: "Con Hubsho integrado a nuestro WMS, el flujo entre pedidos y almacén es automático. Antes teníamos que revisar cada orden manualmente; ahora, todo entra listo para surtirse.",
    author: "Carlos Mendoza",
    role: "Director de Operaciones",
    company: "Distribuidora del Norte"
  },
  {
    quote: "Nuestros clientes ahora pueden hacer pedidos 24/7 desde la app. Aumentamos ventas 40% en el primer trimestre sin necesidad de contratar más personal.",
    author: "Ana Martínez",
    role: "Gerente General",
    company: "Ferretería Central"
  },
  {
    quote: "La mejor decisión fue dejar de depender de marketplaces. Ahora tenemos nuestra propia marca digital, sin comisiones y con control total sobre nuestros clientes.",
    author: "Roberto Sánchez",
    role: "Fundador",
    company: "Materiales Premium"
  }
];
---

<section id="testimonios" class="section testimonials">
  <div class="container">
    <div class="testimonials-header">
      <h2 class="section-title">
        Historias de éxito de nuestros clientes
      </h2>
      <p class="section-subtitle">
        Descubre cómo negocios como el tuyo están creciendo con Hubsho.
      </p>
    </div>

    <div class="testimonials-grid">
      {testimonials.map((testimonial, index) => (
        <div class="testimonial-card">
          <div class="quote-icon">
            <svg width="40" height="32" viewBox="0 0 40 32" fill="none">
              <path d="M0 32V17.5C0 7.625 4.5 0 13.5 0V5C8.5 5 5.5 9.125 5.5 17.5V20H13.5V32H0Z" fill="var(--color-primary)" opacity="0.1"/>
              <path d="M21.5 32V17.5C21.5 7.625 26 0 35 0V5C30 5 27 9.125 27 17.5V20H35V32H21.5Z" fill="var(--color-primary)" opacity="0.1"/>
            </svg>
          </div>
          <blockquote class="testimonial-quote">
            "{testimonial.quote}"
          </blockquote>
          <div class="testimonial-author">
            <div class="author-avatar">
              {testimonial.author.charAt(0)}
            </div>
            <div class="author-info">
              <div class="author-name">{testimonial.author}</div>
              <div class="author-role">{testimonial.role}</div>
              <div class="author-company">{testimonial.company}</div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .testimonials {
    background: white;
  }

  .testimonials-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto calc(var(--spacing-unit) * 8);
  }

  .testimonials-header .section-subtitle {
    margin: 0 auto;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: calc(var(--spacing-unit) * 4);
  }

  .testimonial-card {
    background: var(--color-surface);
    padding: calc(var(--spacing-unit) * 4);
    border-radius: var(--border-radius);
    border: 1px solid var(--color-border);
    position: relative;
    transition: all 0.3s ease;
  }

  .testimonial-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
  }

  .quote-icon {
    margin-bottom: calc(var(--spacing-unit) * 2);
  }

  .testimonial-quote {
    font-size: 1.0625rem;
    line-height: 1.7;
    color: var(--color-text);
    margin-bottom: calc(var(--spacing-unit) * 3);
    font-style: italic;
  }

  .testimonial-author {
    display: flex;
    gap: calc(var(--spacing-unit) * 2);
    align-items: center;
  }

  .author-avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--color-primary);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  .author-name {
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 2px;
  }

  .author-role {
    font-size: 0.875rem;
    color: var(--color-text-light);
  }

  .author-company {
    font-size: 0.875rem;
    color: var(--color-primary);
    font-weight: 500;
  }

  @media (max-width: 768px) {
    .testimonials-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
