---
const plans = [
  {
    name: "Starter",
    price: "Ideal para iniciar",
    description: "Implementación base",
    features: [
      "Tienda web responsive",
      "Hasta 100 productos",
      "1 usuario administrador",
      "Reportes básicos",
      "Soporte por email",
      "Almacenamiento 5GB"
    ],
    cta: "Solicitar demo",
    ctaNote: "Sin compromiso · Demo guiada",
    highlighted: false
  },
  {
    name: "Professional",
    price: "Plan más elegido",
    description: "Escalable según tu operación",
    features: [
      "Todo en Starter, más:",
      "App móvil Android",
      "Productos ilimitados",
      "Hasta 5 usuarios",
      "Precios personalizados",
      "Integraciones básicas",
      "Reportes avanzados",
      "Soporte prioritario 24/7",
      "Almacenamiento 50GB"
    ],
    cta: "Solicitar demo",
    ctaNote: "Recomendado para equipos en crecimiento",
    highlighted: true
  },
  {
    name: "Enterprise",
    price: "Inversión personalizada",
    description: "Según integraciones y volumen",
    features: [
      "Todo en Professional, más:",
      "Usuarios ilimitados",
      "Integraciones personalizadas",
      "API completa",
      "Servidor dedicado",
      "SLA garantizado 99.9%",
      "Gerente de cuenta dedicado",
      "Capacitación personalizada",
      "Almacenamiento ilimitado"
    ],
    cta: "Contactar ventas",
    ctaNote: "Evaluamos tu operación y alcance",
    highlighted: false
  }
];
---

<section id="planes" class="section pricing">
  <div class="container pt-16 sm:pt-20 lg:pt-28">
    <div class="pricing-header">
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 mb-4 sm:mb-6">
        Cada negocio es distinto. Hubsho se cotiza según tu volumen, integraciones y alcance.
      </h2>
      <p class="section-subtitle">
        Sin comisiones por venta. Sin costos ocultos. Cancela cuando quieras.
      </p>
    </div>

    <div class="pricing-grid">
      {plans.map(plan => (
        <div class={`pricing-card ${plan.highlighted ? 'highlighted' : ''}`}>
          {plan.highlighted && (
            <div class="badge">Más popular</div>
          )}
          <div class="plan-header">
            <h3 class="plan-name">{plan.name}</h3>
            <p class="plan-description">{plan.description}</p>
            <div class="plan-price">

              <span class="price-amount">{plan.price}</span>
              
            </div>
          </div>

          <ul class="plan-features">
            {plan.features.map(feature => (
              <li>
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <circle cx="10" cy="10" r="10" fill="var(--color-primary)" opacity="0.1"/>
                  <path d="M6 10L9 13L14 7" stroke="var(--color-primary)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                <span>{feature}</span>
              </li>
            ))}
          </ul>

          <a href="#demo" class={`plan-cta ${plan.highlighted ? 'cta-primary' : 'cta-secondary'}`}>
            {plan.cta}
          </a>
          {plan.ctaNote && (
            <p class="cta-note">{plan.ctaNote}</p>
          )}
        </div>
      ))}
    </div>

    <div class="pricing-note">
      <p>
        <svg class="pricing-note-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect x="2" y="5" width="20" height="14" rx="2" fill="var(--color-primary)" opacity="0.2" stroke="var(--color-primary)" stroke-width="1.5"/>
          <rect x="2" y="5" width="20" height="4" rx="2" fill="var(--color-primary)" opacity="0.3"/>
          <circle cx="6" cy="16" r="1" fill="var(--color-primary)"/>
        </svg>
        Aceptamos todas las tarjetas de crédito y débito. Facturación automática mensual.
      </p>
      <p>
        <svg class="pricing-note-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M7 10V8C7 5.24 9.24 3 12 3C14.76 3 17 5.24 17 8V10" stroke="var(--color-primary)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M5 10H19C20.1 10 21 10.9 21 12V19C21 20.1 20.1 21 19 21H5C3.9 21 3 20.1 3 19V12C3 10.9 3.9 10 5 10Z" fill="var(--color-primary)" opacity="0.2" stroke="var(--color-primary)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          <circle cx="12" cy="15" r="1" fill="var(--color-primary)"/>
        </svg>
        Garantía de devolución de 30 días sin preguntas.
      </p>
    </div>
  </div>
</section>

<style>
    .cta-note {
    margin-top: calc(var(--spacing-unit) * 1.5);
    text-align: center;
    font-size: 0.875rem;
    color: var(--color-text-light);
    line-height: 1.4;
  }
  .pricing-card.highlighted .cta-note {
    color: rgba(17, 24, 39, 0.7);
  }


  .pricing {
    background: var(--color-surface);
  }

  .pricing-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto calc(var(--spacing-unit) * 8);
  }

  .pricing-header .section-subtitle {
    margin: 0 auto;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: calc(var(--spacing-unit) * 4);
    margin-bottom: calc(var(--spacing-unit) * 6);
  }

  .pricing-card {
    background: white;
    border-radius: var(--border-radius);
    padding: calc(var(--spacing-unit) * 4);
    border: 2px solid var(--color-border);
    position: relative;
    transition: all 0.3s ease;
  }

  .pricing-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
  }

  .pricing-card.highlighted {
    border-color: var(--color-primary);
    box-shadow: 0 8px 30px rgba(0, 102, 255, 0.15);
  }

  .badge {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--color-primary);
    color: white;
    padding: calc(var(--spacing-unit) * 1) calc(var(--spacing-unit) * 2);
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .plan-header {
    text-align: center;
    margin-bottom: calc(var(--spacing-unit) * 4);
    padding-bottom: calc(var(--spacing-unit) * 4);
    border-bottom: 1px solid var(--color-border);
  }

  .plan-name {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: calc(var(--spacing-unit) * 1);
  }

  .plan-description {
    color: var(--color-text-light);
    font-size: 0.9375rem;
    margin-bottom: calc(var(--spacing-unit) * 3);
  }

  .plan-price {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 4px;
  }

  .price-currency {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-text-light);
    margin-top: calc(var(--spacing-unit) * 1);
  }

  .price-amount {
    font-size: 3rem;
    font-weight: 700;
    line-height: 1;
    color: var(--color-text);
  }

  .price-period {
    font-size: 1rem;
    color: var(--color-text-light);
    align-self: flex-end;
    margin-bottom: 8px;
  }

  .plan-features {
    list-style: none;
    margin-bottom: calc(var(--spacing-unit) * 4);
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-unit) * 1.5);
  }

  .plan-features li {
    display: flex;
    align-items: flex-start;
    gap: calc(var(--spacing-unit) * 1.5);
    font-size: 0.9375rem;
  }

  .plan-features svg {
    flex-shrink: 0;
    margin-top: 2px;
  }

  .plan-cta {
    display: block;
    width: 100%;
    padding: calc(var(--spacing-unit) * 2);
    text-align: center;
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .cta-primary {
    background: var(--color-primary);
    color: white;
  }

  .cta-primary:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
  }

  .cta-secondary {
    background: transparent;
    color: var(--color-text);
    border: 2px solid var(--color-border);
  }

  .cta-secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .pricing-note {
    text-align: center;
    color: var(--color-text-light);
    font-size: 0.9375rem;
    line-height: 2;
  }

  .pricing-note p {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
  }

  .pricing-note-icon {
    width: 20px;
    height: 20px;
    flex-shrink: 0;
  }

  @media (max-width: 968px) {
    .pricing-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
      margin-left: auto;
      margin-right: auto;
    }
  }
</style>
