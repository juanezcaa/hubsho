---
const plans = [
  {
    name: "Starter",
    price: "2,999",
    period: "mes",
    description: "Perfecto para empezar a vender en línea",
    features: [
      "Tienda web responsive",
      "Hasta 100 productos",
      "1 usuario administrador",
      "Reportes básicos",
      "Soporte por email",
      "Almacenamiento 5GB"
    ],
    cta: "Comenzar ahora",
    highlighted: false
  },
  {
    name: "Professional",
    price: "5,999",
    period: "mes",
    description: "Para negocios en crecimiento",
    features: [
      "Todo en Starter, más:",
      "App móvil Android",
      "Productos ilimitados",
      "Hasta 5 usuarios",
      "Precios personalizados",
      "Integraciones básicas",
      "Reportes avanzados",
      "Soporte prioritario 24/7",
      "Almacenamiento 50GB"
    ],
    cta: "Solicitar demo",
    highlighted: true
  },
  {
    name: "Enterprise",
    price: "Personalizado",
    period: "",
    description: "Para operaciones a gran escala",
    features: [
      "Todo en Professional, más:",
      "Usuarios ilimitados",
      "Integraciones personalizadas",
      "API completa",
      "Servidor dedicado",
      "SLA garantizado 99.9%",
      "Gerente de cuenta dedicado",
      "Capacitación personalizada",
      "Almacenamiento ilimitado"
    ],
    cta: "Contactar ventas",
    highlighted: false
  }
];
---

<section id="planes" class="section pricing">
  <div class="container">
    <div class="pricing-header">
      <h2 class="section-title">
        Planes transparentes que crecen contigo
      </h2>
      <p class="section-subtitle">
        Sin comisiones por venta. Sin costos ocultos. Cancela cuando quieras.
      </p>
    </div>

    <div class="pricing-grid">
      {plans.map(plan => (
        <div class={`pricing-card ${plan.highlighted ? 'highlighted' : ''}`}>
          {plan.highlighted && (
            <div class="badge">Más popular</div>
          )}
          <div class="plan-header">
            <h3 class="plan-name">{plan.name}</h3>
            <p class="plan-description">{plan.description}</p>
            <div class="plan-price">
              <span class="price-currency">$</span>
              <span class="price-amount">{plan.price}</span>
              {plan.period && <span class="price-period">/{plan.period}</span>}
            </div>
          </div>

          <ul class="plan-features">
            {plan.features.map(feature => (
              <li>
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <circle cx="10" cy="10" r="10" fill="var(--color-primary)" opacity="0.1"/>
                  <path d="M6 10L9 13L14 7" stroke="var(--color-primary)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                <span>{feature}</span>
              </li>
            ))}
          </ul>

          <a href="#demo" class={`plan-cta ${plan.highlighted ? 'cta-primary' : 'cta-secondary'}`}>
            {plan.cta}
          </a>
        </div>
      ))}
    </div>

    <div class="pricing-note">
      <p>💳 Aceptamos todas las tarjetas de crédito y débito. Facturación automática mensual.</p>
      <p>🔒 Garantía de devolución de 30 días sin preguntas.</p>
    </div>
  </div>
</section>

<style>
  .pricing {
    background: var(--color-surface);
  }

  .pricing-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto calc(var(--spacing-unit) * 8);
  }

  .pricing-header .section-subtitle {
    margin: 0 auto;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: calc(var(--spacing-unit) * 4);
    margin-bottom: calc(var(--spacing-unit) * 6);
  }

  .pricing-card {
    background: white;
    border-radius: var(--border-radius);
    padding: calc(var(--spacing-unit) * 4);
    border: 2px solid var(--color-border);
    position: relative;
    transition: all 0.3s ease;
  }

  .pricing-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
  }

  .pricing-card.highlighted {
    border-color: var(--color-primary);
    box-shadow: 0 8px 30px rgba(0, 102, 255, 0.15);
  }

  .badge {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--color-primary);
    color: white;
    padding: calc(var(--spacing-unit) * 1) calc(var(--spacing-unit) * 2);
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .plan-header {
    text-align: center;
    margin-bottom: calc(var(--spacing-unit) * 4);
    padding-bottom: calc(var(--spacing-unit) * 4);
    border-bottom: 1px solid var(--color-border);
  }

  .plan-name {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: calc(var(--spacing-unit) * 1);
  }

  .plan-description {
    color: var(--color-text-light);
    font-size: 0.9375rem;
    margin-bottom: calc(var(--spacing-unit) * 3);
  }

  .plan-price {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 4px;
  }

  .price-currency {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-text-light);
    margin-top: calc(var(--spacing-unit) * 1);
  }

  .price-amount {
    font-size: 3rem;
    font-weight: 700;
    line-height: 1;
    color: var(--color-text);
  }

  .price-period {
    font-size: 1rem;
    color: var(--color-text-light);
    align-self: flex-end;
    margin-bottom: 8px;
  }

  .plan-features {
    list-style: none;
    margin-bottom: calc(var(--spacing-unit) * 4);
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-unit) * 1.5);
  }

  .plan-features li {
    display: flex;
    align-items: flex-start;
    gap: calc(var(--spacing-unit) * 1.5);
    font-size: 0.9375rem;
  }

  .plan-features svg {
    flex-shrink: 0;
    margin-top: 2px;
  }

  .plan-cta {
    display: block;
    width: 100%;
    padding: calc(var(--spacing-unit) * 2);
    text-align: center;
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .cta-primary {
    background: var(--color-primary);
    color: white;
  }

  .cta-primary:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
  }

  .cta-secondary {
    background: transparent;
    color: var(--color-text);
    border: 2px solid var(--color-border);
  }

  .cta-secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .pricing-note {
    text-align: center;
    color: var(--color-text-light);
    font-size: 0.9375rem;
    line-height: 2;
  }

  @media (max-width: 968px) {
    .pricing-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
      margin-left: auto;
      margin-right: auto;
    }
  }
</style>
